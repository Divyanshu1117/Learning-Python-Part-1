# Creating a tuple
a = (2, 4, 5, 3, 2)

# Trying to change the value at index 0
a[0] = 45  # Error: TypeError - 'tuple' object does not support item assignment
# Tuples are immutable (cannot be modified after creation)